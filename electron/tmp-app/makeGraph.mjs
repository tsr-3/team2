/**
 * make graph
 * @param {[{student: string, ratio: number}]} arr
 * @return {undefined}
 */
function make_graph(arr) {
  let hr = document.querySelectorAll("hr")[1];
  let canvas = document.createElement("canvas");

}


let arr = [
  {student:'id', ratio: 99.8},
  {student:'id', ratio: 99.8},
  {student:'id', ratio: 99.8},
]
